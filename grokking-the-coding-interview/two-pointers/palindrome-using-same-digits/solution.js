export const findNextPermutation = () => {


    return false
}

export const findNextPalindrome = (numStr) => {

    const n = numStr.length

    if (n === 1) {
        return ""
    }

    let halfLength = Math.floor(n / 2)
    let leftHalf = numStr.slice(0, halfLength).split()

    if (!findNextPermutaion) {
        return ""
    }


}